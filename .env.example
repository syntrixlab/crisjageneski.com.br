# =====================================================
# CONFIGURAÇÃO DE PRODUÇÃO - HOSTINGER
# =====================================================
# Copie este arquivo para .env e preencha com valores reais

# Server
NODE_ENV=production
# PORT - NÃO CONFIGURE (Hostinger define automaticamente via process.env.PORT)
DATABASE_URL="postgresql://usuario:senha@host:porta/database"
JWT_SECRET="gere_uma_chave_aleatoria_segura_minimo_16_caracteres"
SUPABASE_URL="https://seuproject.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="sua_service_role_key_aqui"
SUPABASE_STORAGE_BUCKET="site-media"
REDIS_URL="rediss://:senha@redis-host:porta" # (opcional)
REDIS_PREFIX="cris-site"
CACHE_TTL_NAV=3600
CACHE_TTL_HOME=3600
CACHE_TTL_PAGE=600
CACHE_TTL_POST=600
CACHE_TTL_POSTS_LIST=600
UPLOAD_MAX_FILE_SIZE_MB=5
ALLOWED_IMAGE_MIME_TYPES="image/jpeg,image/png,image/webp"
ADMIN_EMAIL=admin@seudominio.com
ADMIN_PASSWORD=senha_super_segura_altere_em_producao

# Client - NÃO É NECESSÁRIO EM PRODUÇÃO (apenas desenvolvimento)
# VITE_API_URL - Em produção, o build do Vite usa /api (relativo)

