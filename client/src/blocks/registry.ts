import type { BlockType } from '@/types';
import type { BlockConfig } from './_shared/types';

// Preenchido na Task 15. Stub vazio por enquanto.
export const blockRegistry = {} as Record<BlockType, BlockConfig>;
